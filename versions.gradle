ext.deps = [:]
def versions = [:]
versions.appcompat = "1.2.0-beta01"

ext.versions = versions

def support = [:]
support.app_compat = "androidx.appcompat:appcompat:$versions.appcompat"
deps.support = support